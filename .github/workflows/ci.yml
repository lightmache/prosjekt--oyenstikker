name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install fastapi uvicorn psycopg2-binary sentence-transformers pydantic python-dotenv ruff

      - name: Lint with ruff
        run: ruff check fuse.py main.py ingest_vinton.py

      - name: Validate docker-compose
        run: |
          pip install docker compose
          docker compose config

  docker-build:
    runs-on: ubuntu-latest
    needs: lint-and-test
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t oyenstikker:ci . || echo "No Dockerfile yet - build skipped"

      - name: Verify compose services defined
        run: |
          docker compose config --services
